# Everything here is meant to be crawled.
User-agent: *
Disallow:

# The sitemap is generated by the gateway at /sitemap.xml (SitemapController).
# A Sitemap: line has to be an absolute URL, and this file is static, so it
# cannot be written until this origin has its domain. Uncomment and fill in:
#
# Sitemap: https://example.com/sitemap.xml
